spring:
  # Data Source - Postgresql
  datasource:
    name: jdbc/DataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db:5432/venus
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  use-forward-headers: true
frontend:
  base-url: http://www.venusapp.ee